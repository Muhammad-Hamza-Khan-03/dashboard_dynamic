declare module 'echarts-gl' {
    // This is a global module declaration that adds the GL components to ECharts
    // The module doesn't export any named components, but registers them globally
    const echartsGL: any;
    export default echartsGL;
  }
  